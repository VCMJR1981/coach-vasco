# Photos

Drop the real files here with these exact names. Every slot fails soft:
if a file is missing the element removes itself, so the page never shows
a broken image.

| Filename | Shape | Where it appears | Which photo |
|---|---|---|---|
| `hero-surfskate-sunset.jpg` | landscape, 1600px+ wide | full-bleed behind the hero headline | the sunset surfskater with the cones |
| `surfskate-sunset-group.jpg` | landscape, wide crop | band between "Work with us" and "Retreats" | the second sunset session shot |
| `retreat-aljezur-group.jpg` | landscape, wide crop | band above the story section | the group on the boardwalk with boards |
| `surf-lesson-beach.jpg` | 16:9 | inside the method section | the beach lesson seen from above |
| `vasco.jpg` | square, 300px+ | the coach strip in the hero | Vasco, cropped square |

Hero crop: the focal point sits at 72% across, 50% down on desktop and
68%/40% on mobile. Frame the rider right of centre or adjust
`.hero-photo img{object-position}`.

Export JPEG, quality ~80, no wider than 2000px. Nothing here needs to be
bigger than that.
